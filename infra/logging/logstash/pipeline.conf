input {
  beats { port => 5044 }
}
filter {
  json { source => "message" }
  mutate { add_field => { "platform" => "algosaas" } }
}
output {
  elasticsearch {
    hosts => ["http://elasticsearch-master:9200"]
    index => "algosaas-logs-%{+YYYY.MM.dd}"
  }
}
